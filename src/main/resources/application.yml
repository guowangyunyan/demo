spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yuan-student?serverTimezone=UTC
    username: root
    password: root
mybatis:
  mapperLocations: classpath:mapper/**/*.xml

